algoritmo "AproveitamentoAluno"

var

     n1, n2, m: real

inicio

      Escreva ("Digite a nota do aluno: ")
      Leia (n1)
      Escreva ("Digite a segunda nota do aluno: ")
      Leia (n2)
      
      m <- (n1+n2)/2
      
      Escreval ("A média desse aluno é ", m)

      Se (m >= 9) e (m <= 10) entao
         Escreval ("Aluno categoria A")
       senao
         se (m >= 8) e (m <= 9) entao
            Escreval ("Aluno categoria B")
        senao
         se (m >= 7) e (m <= 8) entao
             Escreval ("Aluno categoria C")
        senao
         se (m >= 6) e (M <= 7) entao
             Escreval ("Aluno categoria D")
        senao
         se (m >= 5) e (m = 6) entao
             Escreval ("Aluno categoria E")
        senao
            Escreval("Aluno categoria F")
            
                  FimSe
              FimSe
          FimSe
      FimSe
  Fimse
fimalgoritmo

algoritmo "E17"

var

   suco, preco, result: real
   
inicio

  Escreval("Digite a quantidade de sucos: ")
  Leia (suco)
  preco <- 5
  
    Se (suco > 10) entao
       preco <- 4
    Fimse
  
  result <- (suco * preco)
  
  Escreval ("Valor a ser pago: ", result)

fimalgoritmo
